{
  "name": "tubes-apb-be",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun run test:unit && bun run contract:check && bun run test:integration",
    "test:unit": "NODE_ENV=test bun test tests/unit",
    "test:integration": "NODE_ENV=test AUTO_MIGRATE=true DATABASE_URL=postgres://finu:finu@localhost:5433/finu bun test tests/integration/auth.test.ts && NODE_ENV=test AUTO_MIGRATE=true DATABASE_URL=postgres://finu:finu@localhost:5433/finu bun test tests/integration/protected-error.test.ts && NODE_ENV=test AUTO_MIGRATE=true DATABASE_URL=postgres://finu:finu@localhost:5433/finu bun test tests/integration/categories-finance.test.ts && NODE_ENV=test AUTO_MIGRATE=true DATABASE_URL=postgres://finu:finu@localhost:5433/finu bun test tests/integration/profile-activities.test.ts && NODE_ENV=test AUTO_MIGRATE=true DATABASE_URL=postgres://finu:finu@localhost:5433/finu bun test tests/integration/demo-seed.test.ts",
    "contract:check": "bun run scripts/check-openapi-contract.ts",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "hono": "^4.12.27",
    "jose": "^6.1.3",
    "nodemailer": "^9.0.1",
    "postgres": "^3.4.7",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/nodemailer": "^7.0.4",
    "drizzle-kit": "^0.31.10",
    "typescript": "^5.9.3"
  }
}
